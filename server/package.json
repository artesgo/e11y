{
  "name": "space-station",
  "version": "1.0.0",
  "description": "docker hapi angular mongo",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker build -t e11y-server .",
    "bash": "docker exec -it server_mongo_1 bash",
    "prestart": "npm run build",
    "start": "docker-compose up -d"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^19.1.1",
    "@hapi/inert": "^5.2.2",
    "@hapi/joi": "^16.1.7",
    "mongoose": "^5.7.7"
  }
}
